# Skills 与现有系统的融合分析

> 分析时间：2026-08-09
> 涉及 Skills：`position-sizer` (1.2K installs)、`risk-management` (1.9K installs)、`trading-plan-generator` (1.1K installs)

---

## 一、现有系统能力总览

### 1. 仓位计算器 (calculator.js)

| 功能 | 现状 | 公式/实现 |
|------|------|----------|
| 仓位计算 | ✅ 固定比例 | `positionSize = riskAmount × entryPrice / stopDistance` |
| 风险比例 | ✅ 可选 | 0.5% ~ 10% 或固定金额 |
| 保证金硬上限 | ✅ 单笔 ≤ 可用本金 80% | `positionSize = availableCapital × leverage × 0.8` |
| 聚合硬上限 | ✅ 总保证金 ≤ 本金 90% | `totalMargin = usedMargin + newMargin ≤ capital × 0.9` |
| 强平价校验 | ✅ 止损不能超越强平价 | `stopLoss < liquidationPrice` (做多) |
| 连亏降仓 | ✅ ≥3 笔降 20% | `positionSize *= 0.8` when `lossStreak >= 3` |
| ATR 字段 | ⚠️ 有输入框但未参与计算 | `atrValue` + `atrMultiplier` 只读取，未使用 |
| 分批加权 | ✅ 加权平均入场/止损 | `weightedStopPct = Σ(|bp-bsl|/bp × ba)` |
| 滑点/手续费 | ✅ 已计入净盈亏比 | Market/Stop 用 Taker 费率，Limit 用 Maker |

### 2. 风控中心 (risk.js)

| 功能 | 现状 | 公式/实现 |
|------|------|----------|
| 日亏损监控 | ✅ 信息显示 | 累计今日已平仓 PnL vs `capital × dailyLossLimit%` |
| 回撤监控 | ✅ 信息显示 | 历史最高净值 vs 当前净值 |
| 强平距离 | ✅ 显示安全距离 | `|stopLoss - liquidationPrice| / entryPrice × 100` |
| 品种集中度 | ✅ 信息显示 | 按 symbol 分组统计保证金占比 |
| 杠杆分布 | ✅ 统计 | 按杠杆区间分组计数 |
| 交易频率 | ✅ 柱状图 | 按天统计交易笔数，最长连赢/连亏 streak |
| 聚合保证金 | ⚠️ 仅计算器使用 | risk.js 未做聚合风险检查 |

### 3. 统计分析 (analytics.js)

| 功能 | 现状 | 缺失 |
|------|------|------|
| 策略绩效 | ✅ 胜率/均盈亏/R | 无 Profit Factor / Expectancy |
| 形态绩效 | ✅ 分形态统计 | 同上 |
| 时段分析 | ✅ 亚/欧/美盘 | — |
| 周几分析 | ✅ 周一到周日 | — |
| 持仓时长 | ✅ 分箱统计 | — |
| MAE/MFE | ✅ 散点图 | — |
| 月度盈亏 | ✅ 年度趋势 | — |
| 资金曲线 | ✅ Chart.js 折线 | — |

### 4. 设置 (settings.js)

| 设置项 | 默认值 | 范围 |
|--------|--------|------|
| 账户余额 | 0 | ≥ 0 |
| 单笔风险比例 | 2% | 1% ~ 10% |
| 日亏损上限 | 5% | 1% ~ 50% |
| 最大回撤告警 | 20% | 5% ~ 50% |
| 默认杠杆 | 10x | 1x ~ 125x |
| 维持保证金率 | 0.5% | 0.1% ~ 5% |
| 自定义止损比例 | — | JSON 对象 |
| 心态最低分 | 3 | 1 ~ 5 |

---

## 二、Skills 核心逻辑梳理

### position-sizer 技能

| 原则 | 具体内容 |
|------|----------|
| **1% 规则** | 默认单笔风险 1%，上限 2% |
| **生存优先** | 仓位计算目的是"存活"而非"最大化收益" |
| **向下取整** | 整股模式 floor；分数股模式 floor 到指定精度，确保不超预算 |
| **最严约束获胜** | 多个限制条件时，取最严格的一个 |
| **半凯利** | 不使用满凯利，半凯利即可捕获 75% 增长，风险大幅降低 |
| **组合热量** | 总开口风险不超过账户权益的 6%~8% |
| **凯利公式** | `Kelly% = (WinRate × AvgWin - LossRate × AvgLoss) / AvgWin` |
| **ATR 动态止损** | `stopDistance = ATR × multiplier`（默认 2.0x） |

### risk-management 技能

| 规则 | 成功率 | 样本 | 置信度 |
|------|--------|------|--------|
| 交易频率与表现负相关（牛市中 8 笔 vs 30+ 笔） | 95% | 473 | 50% |
| 2% 风险 + 2:1 盈亏比 + 方向正确 = 成功 | 92% | 200 | 95% |
| 限制日交易 ≤ 10 笔（方向不确定时） | 90% | 84 | 45% |
| 全部 5 个资产为正且涨幅 > 5% → 暂停做空 | 95% | 58 | 45% |
| 短线快速平仓（LONG 优先） | 88% | 50 | 95% |
| 低频交易（< 40 笔/天）优于高频 | 75% | 48 | 95% |

### trading-plan-generator 技能

| 框架 | 关键参数 |
|------|----------|
| **1% 规则** | 单笔风险 1%（推荐），≤ 2%（上限） |
| **2R 最低规则** | 只接 R:R ≥ 2:1 的交易 |
| **最大回撤停止** | 保守 10% / 中等 15% / 激进 20% |
| **日内止损** | 新手 -1.5% / 有经验 -3% |
| **交易频率** | 日内 ≤ 10 笔/天；波段 2~5 笔/周 |
| **连亏处理** | ≥ 3 笔后暂停或降仓至 50%~80% |
| **心态检查** | 愤怒/沮丧/报复心理/分心/疲惫/过度自信 → 禁止交易 |

---

## 三、融合点分析

### 🔴 高优先级融合（核心计算逻辑）

#### 融合点 1：凯利公式计算

**现状**：计算器完全没有凯利计算能力。

**Skills 提供**：
- 完整凯利公式：`Kelly% = (WR × AvgWin - LR × AvgLoss) / AvgWin`
- 半凯利建议（实践标准）
- 基于历史胜率/盈亏比的动态仓位调整

**融合方案**：
1. 在计算器新增「凯利模式」选项卡
2. 用户输入历史胜率、平均盈利、平均亏损
3. 同时显示固定比例结果 + 凯利结果（半凯利）
4. 在结果区用对比表格呈现两种方法的差异

**涉及文件**：`calculator.js`（新增计算逻辑）、`index.html`（新增 UI 表单）

---

#### 融合点 2：ATR 动态止损距离

**现状**：ATR 输入框存在但完全没有参与计算，只作为信息展示。

**Skills 提供**：
- ATR 作为动态波动率代理
- `stopDistance = ATR × multiplier`（默认 2.0x）
- 自动根据市场波动率调整仓位

**融合方案**：
1. 在 `calculate()` 中，当 ATR 值非空时，优先用 ATR 计算止损距离
2. 公式：`stopDistance = atrValue × parseFloat(atrMultiplier)`
3. 用 ATR 计算的止损距离替代手动输入的止损价（或并行列出两个值供用户选择）
4. 当 ATR 模式生效时，在结果区标注「ATR 动态止损」tag

**涉及文件**：`calculator.js`（calculate 函数）、`index.html`（ATR 区域文案）

---

#### 融合点 3：组合热量（Portfolio Heat）检查

**现状**：有单笔 80% 保证金限制和 90% 聚合限制，但没有任何「风险热量」概念。

**Skills 提供**：
- 总开口风险不应超过账户权益的 6%~8%
- `portfolioHeat = Σ(positionRisk / accountEquity) × 100`

**融合方案**：
1. 新增「组合热量」计算：遍历所有未平仓持仓，汇总 `riskAmount / capital`
2. 在计算器中，新开仓前检查：`currentHeat + newTradeRisk ≤ 8%`
3. 在风控中心新增「组合热量」卡片，显示当前总热量和进度条
4. 超过 6% 时黄色警告，超过 8% 时红色禁止

**涉及文件**：`risk.js`（新增 renderPortfolioHeat）、`calculator.js`（新增热量检查）、`index.html`（新增卡片）

---

### 🟡 中优先级融合（规则增强）

#### 融合点 4：R:R 最低 2:1 强制检查

**现状**：盈亏比只是展示，不阻止不符合条件的交易。

**Skills 提供**：
- 只接受 R:R ≥ 2:1 的交易（40% 胜率即可盈利）
- 低于 2:1 的交易建议跳过

**融合方案**：
1. 在 calculate() 中，当 `targetRR < 2` 时，显示红色警告而非仅变色
2. 新增「R:R 不足，建议跳过此交易」的明确提示
3. 在设置中新增「最低盈亏比」选项（默认 2.0）

**涉及文件**：`calculator.js`（calculate 函数）、`settings.js`（新增配置项）、`index.html`（设置表单）

---

#### 融合点 5：日亏损硬止损

**现状**：日亏损上限只是信息显示，不阻止交易。

**Skills 提供**：
- 日亏损达到上限必须立即停止交易
- 新手建议 -1.5%，有经验者 -3%

**融合方案**：
1. 在 calculate() 中检查今日累计亏损是否已达上限
2. 达到上限时：显示「日亏损已达上限，禁止开新仓」并禁用计算按钮
3. 与现有熔断（连亏 3 笔）合并为统一的「交易熔断」系统
4. 在仪表盘显示日亏损进度条（当前值/上限）

**涉及文件**：`calculator.js`（新增熔断检查）、`dashboard.js`（仪表盘进度条）、`risk.js`（日亏损卡片改进）

---

#### 融合点 6：品种/板块集中度硬上限

**现状**：品种集中度只显示，不限制。

**Skills 提供**：
- 单品种仓位不应超过账户的 10%~15%
- 同板块总仓位不应超过 30%~40%

**融合方案**：
1. 在设置中新增「最大单品种占比」和「最大板块占比」配置
2. 在计算器中计算新仓位后，检查是否超过单品种限制
3. 在风险中心增加板块汇总（需要用户标注板块标签）

**涉及文件**：`settings.js`（新增配置）、`calculator.js`（新增检查）、`risk.js`（新增板块聚合）

---

#### 融合点 7：交易频率指导

**现状**：有交易频率统计和柱状图，但没有指导。

**Skills 提供**：
- 最佳频率：8 笔/天
- 方向不确定时 ≤ 10 笔/天
- 高频（> 165 笔/天）成功率仅 50%

**融合方案**：
1. 在仪表盘新增「建议交易频率」指标
2. 在风控中心频率卡片中增加参考线和颜色标注
3. 显示「今日建议最大交易次数」基于当前连续交易天数
4. 在每日统计中添加「效率评分」= 日均交易数 / 建议上限

**涉及文件**：`risk.js`（频率卡片增强）、`dashboard.js`（新增指标）、`css/dashboard.css`（样式）

---

#### 融合点 8：心态评分影响仓位

**现状**：心态评分只记录，不参与计算。

**Skills 提供**：
- 情绪化交易是亏损的主因之一
- 低心态评分时应降低仓位或禁止交易

**融合方案**：
1. 在 calculate() 中，当 mindsetScore < 3 时自动降低仓位 20%
2. 当 mindsetScore = 1 时，直接禁止计算并显示「不建议交易」提示
3. 与连亏降仓逻辑合并：`adjustment = max(streakAdjust, mindsetAdjust)`

**涉及文件**：`calculator.js`（新增心态检查）、`index.html`（UI 提示）

---

### 🟢 低优先级融合（分析与追踪增强）

#### 融合点 9：完善绩效指标

**缺失指标**：
| 指标 | 公式 | 用途 |
|------|------|------|
| Profit Factor | 总盈利 / 总亏损 | > 1.5 良好，> 2.0 优秀 |
| Expectancy | WR × AvgWin - LR × AvgLoss | 必须 > 0 才可盈利 |
| Sharpe Ratio | 平均收益 / 收益标准差 | > 1.0 良好 |
| Recovery Factor | 总盈利 / 最大回撤 | > 1.0 良好 |

**融合方案**：在 analytics.js 的 `renderStrategyChart` 和 `renderPatternChart` 中添加这些指标列。

---

#### 融合点 10：交易计划生成器框架

**Skills 提供**：完整的交易计划框架（目标、规则、心态、复盘）

**融合方案**：
1. 在「开仓计划」页面顶部新增交易计划概览区
2. 从设置中读取用户的风险参数，自动生成计划摘要
3. 显示当前计划 vs 实际执行的合规率

---

#### 融合点 11：复盘中心增强

**Skills 提供**：系统化的复盘框架

**融合方案**：
1. 在 review.js 中增加 MAE/MFE 分析（已有数据但未展示）
2. 增加「最佳/最差交易日」分析
3. 增加「止损 vs 止盈比例」分析

---

## 四、优先级排序与实施建议

### 第一阶段：核心计算逻辑融合（2~3 天）

| 序号 | 融合点 | 改动范围 | 预期收益 |
|------|--------|----------|----------|
| 1 | ATR 动态止损 | calculator.js + index.html | 提高止损合理性 |
| 2 | 组合热量检查 | risk.js + calculator.js | 防止过度开仓 |
| 3 | 凯利公式 | calculator.js + index.html | 科学仓位决策 |

### 第二阶段：规则增强（2~3 天）

| 序号 | 融合点 | 改动范围 | 预期收益 |
|------|--------|----------|----------|
| 4 | R:R 最低检查 | calculator.js + settings.js | 提升交易质量筛选 |
| 5 | 日亏损硬止损 | calculator.js + dashboard.js | 防止爆仓日 |
| 6 | 品种集中度限制 | settings.js + calculator.js + risk.js | 降低集中度风险 |
| 7 | 交易频率指导 | risk.js + dashboard.js | 优化交易效率 |
| 8 | 心态仓位影响 | calculator.js | 减少情绪化交易 |

### 第三阶段：分析与复盘增强（3~5 天）

| 序号 | 融合点 | 改动范围 | 预期收益 |
|------|--------|----------|----------|
| 9 | 绩效指标完善 | analytics.js | 更全面的数据分析 |
| 10 | 交易计划框架 | index.html + planner.js | 系统化交易管理 |
| 11 | 复盘增强 | review.js | 更好的改进闭环 |

---

## 五、关键公式对照表

| 公式 | 当前系统 | Skills 建议 | 差异 |
|------|----------|------------|------|
| 仓位大小 | `riskAmount × entry / stopDist` | 同左 + ATR + 凯利 | Skills 多两个方法 |
| 风险比例默认 | 2% | 1%（推荐）/ 2%（上限） | 当前偏高 1% |
| 组合风险上限 | 90% 保证金 | 6~8% 风险热量 | 当前约束过松 |
| 连亏降仓 | ≥3 笔降 20% | ≥3 笔降 20%~50% | 一致 |
| 最低 R:R | 无强制 | ≥ 2:1 | 当前无检查 |
| 日亏损止损 | 软警告 | 硬停止 | 当前不够严格 |
| 心态检查 | 仅记录 | 影响仓位 | 当前未使用 |

---

## 六、技术实现注意事项

1. **向后兼容**：所有新功能默认关闭，通过设置开关启用
2. **数据迁移**：新增字段（如 sector、portfolioHeat）需要有兜底逻辑
3. **性能考虑**：组合热量计算需 O(n)，n 为未平仓数，通常 < 20，无性能问题
4. **设置迁移**：`SETTINGS_DEFAULTS` 中新增字段，`loadSettings()` 已处理兼容
5. **前端计算优先**：避免引入 Python 依赖，所有计算用纯 JavaScript 实现
